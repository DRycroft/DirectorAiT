# .env.example — placeholders only. DO NOT COMMIT REAL SECRETS.
# Use this as a template. Copy to .env (locally / server) and fill with real values.
# Client-exposed keys (Vite): prefix with VITE_ so they are intentionally public.
# Server-only keys: keep them in server env / GitHub Actions / Vercel secrets only.

# --- Supabase (client) ---
# IMPORTANT: Use VITE_SUPABASE_PUBLISHABLE_KEY (NOT ANON_KEY) for consistency
VITE_SUPABASE_URL="https://your-project.supabase.co"
VITE_SUPABASE_PUBLISHABLE_KEY="REPLACE_WITH_ANON_PUBLIC_KEY"
VITE_SUPABASE_PROJECT_ID="your-project-ref"

# --- Supabase (server-only) ---
SUPABASE_SERVICE_ROLE="REPLACE_WITH_SERVICE_ROLE_KEY"   # SERVER ONLY: NEVER expose this to client-side code

# --- Monitoring / Error tracking ---
SENTRY_DSN="REPLACE_WITH_SENTRY_DSN"                    # server or client depending on usage

# --- App secrets ---
CRON_SECRET="REPLACE_WITH_CRON_SECRET"                  # keep on server side

# IMPORTANT: Do NOT commit real secrets. Use GitHub Actions / Vercel / Supabase secret stores.
